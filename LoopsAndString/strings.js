// tempelate literals

let object1 = {
  product: "pen",
  price: 10,
};

console.log("the price of ", object1.product, "is ", object1.price, "rupee");

let output = `the price of ${object1.product} is ${object1.price}` // Template literals // string interpolation

console.log(output)
